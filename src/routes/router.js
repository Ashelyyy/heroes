// 导入vue-router
import VueRouter from 'vue-router';
// 导入Vue
import Vue from 'vue';

// 注册VueRouter插件
Vue.use(VueRouter);

// 创建路由对象
var router = new VueRouter({
  // 路由规则
  routes: [

  ]
});

// 导出路由
export default router;

